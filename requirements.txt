# lecture-prism 의존성
#
# 기본 파이프라인(main.py)은 표준 라이브러리만으로 동작합니다 (mock 모드).
# 아래는 "실제 연동"을 켤 때 필요한 선택적 패키지입니다.

# 대시보드 (dashboard.py) — 필수에 가까움
fastapi>=0.110
uvicorn>=0.29

# 실제 LLM 분석 (analysis.py)
# ChatGPT 구독은 공식 Codex CLI를 사용하므로 Python 패키지가 필요하지 않습니다.
# 별도 OpenAI API 키 경로를 선택할 때만 openai 패키지를 사용합니다.
openai>=1.40
# cores/chatgpt_proxy 참고 회귀 테스트용 선택 의존성
aiohttp>=3.9

# 실데이터 스크리닝 + 분석 (screening.py --real / analysis.py / data_source.py)
# 무료·무로그인으로 한국주식 가격·거래량·재무·뉴스·지수를 가져옵니다(야후 파이낸스).
# 미설치 시 analysis.py는 자동으로 mock 데이터로 폴백합니다.
yfinance>=0.2.40

# KIS 모의·실전 매수 주문 브리지 (brokers/kis.py)
# 기본 mock 실행에는 필요하지 않으며, KIS 연결을 선택한 경우에만 사용합니다.
pandas
tenacity
requests
websockets
PyYAML
pycryptodome
cryptography
